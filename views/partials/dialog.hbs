<dialog class="dialog dialog-{{slug}}">

    {{!-- flex on the dialog itself breaks it, so we use an extra wrapper --}}
    <section>

        <h2>{{title}}</h2>

        {{#if image_url}}
            <img src="{{image_url}}" alt="{{image_alt}}">
        {{/if}}

        {{{message}}}

        <div class="dialog__controls">
            {{#if (eq button_type "form")}}

                {{#if (eq slug "assign_users")}}

                    <ul>
                        <li>
                            <img src="splash.it/40/40" alt="">
                            <span>John Doe</span>
                            <i class="icon fa-solid fa-check-circle"></i>
                        </li>
                    </ul>

                    <form method="dialog">
                        <button class="btn--cancel">Cancel</button>
                    </form>

                    <a href="{{button_href}}" class="btn">{{button}}</a>
                {{else}}
                    <form method="dialog">
                        <button class="btn">{{button}}</button>
                    </form>
                {{/if}}

            {{else if (eq button_type "link")}}

                <form method="dialog">
                    <button class="btn--cancel">Cancel</button>
                </form>

                <a href="{{button_href}}" class="btn">{{button}}</a>


            {{else}}

                <form method="dialog">
                    <button class="btn--cancel">Cancel</button>
                </form>

                <a href="{{button_href}}" class="btn">{{button}}</a>

            {{/if}}
        </div>


    </section>

</dialog>
